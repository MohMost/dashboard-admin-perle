# Base URL of the PERLEDESLYS backend (perledeslys-backend, NestJS).
# The dashboard calls ${VITE_API_URL}/api/... (auth, settings, …).
# Local dev: the backend runs on http://localhost:3000. Point this at the
# deployed API in production.
VITE_API_URL=http://192.168.100.14:3000
